#include <iostream>
#include <string>
using namespace std;
int main(){
    double a, b, c, d;
    cout << "cuantas horas de trabajo tiene ";
    cin >> a;
    cout << "cuanto pagan por hora ";
    cin >> b;
    c = a * b;
    if (a > 40){
        d = c + (c*10/100);
        cout << "Por su trabajo se le pagara " << d << endl;
    }
    else{
         cout << "Por su trabajo se le pagara " << c << endl;
    }
    return 0;
}#
